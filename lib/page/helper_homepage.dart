import 'package:flutter/material.dart';

class HelperHome extends StatelessWidget {
  const HelperHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Helper Home Page")));
  }
}
